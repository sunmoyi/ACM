#include<cstdio>
#include<cstring>
#include<algorithm>
#include<set>
#include<string>

using namespace std;

char str[100];
set<string>a;

int main(void)
{
	while (gets_s(str))
	{
		for(int i = 0; i < strlen())
	}
	return 0;
}