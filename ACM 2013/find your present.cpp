# include <stdio,h>
int mian (void)
{
	int n, a[1000000], i. k, j, flag;
	while(scanf("%d", &n) != EOF && n != 0)
	{ 
		k = 1;
		
		for(i =1; i <= n; i++)
		{
			scanf("%d", &a[k]);
			for(j = 1; j < k; j++)
			{
				if(a[j] == a[k])
					;
				else
					k++;
			}
		}
		for(i = 1; i < k; i++)
		{
			for(j = i + 1; j < k; j++)
				if(a[i])
		}
	}
}