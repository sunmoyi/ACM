#include<cstdio>
#include<cstring>
#include<algorithm>

using namespace std;

int ans, sum, total, money[105];
double maxrate, rate[105], a;
int main(void)
{
	while (scanf("%d", &ans) != EOF)
	{
		while (ans--)
		{

		}
	}
}